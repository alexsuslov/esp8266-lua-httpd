# esp8266-lua-httpd
Simple http server

# Install
```bash
wget https://raw.githubusercontent.com/alexsuslov/esp8266-lua-httpd/master/httpd.lua
```

# Use
```lua
httpd = require("httpd")
http.init(80)
```